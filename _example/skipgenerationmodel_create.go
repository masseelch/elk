// Code generated by entc, DO NOT EDIT.

package _example

import (
	"context"
	"errors"
	"fmt"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/masseelch/elk/_example/skipgenerationmodel"
)

// SkipGenerationModelCreate is the builder for creating a SkipGenerationModel entity.
type SkipGenerationModelCreate struct {
	config
	mutation *SkipGenerationModelMutation
	hooks    []Hook
}

// SetName sets the name field.
func (sgmc *SkipGenerationModelCreate) SetName(s string) *SkipGenerationModelCreate {
	sgmc.mutation.SetName(s)
	return sgmc
}

// Mutation returns the SkipGenerationModelMutation object of the builder.
func (sgmc *SkipGenerationModelCreate) Mutation() *SkipGenerationModelMutation {
	return sgmc.mutation
}

// Save creates the SkipGenerationModel in the database.
func (sgmc *SkipGenerationModelCreate) Save(ctx context.Context) (*SkipGenerationModel, error) {
	var (
		err  error
		node *SkipGenerationModel
	)
	if len(sgmc.hooks) == 0 {
		if err = sgmc.check(); err != nil {
			return nil, err
		}
		node, err = sgmc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SkipGenerationModelMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = sgmc.check(); err != nil {
				return nil, err
			}
			sgmc.mutation = mutation
			node, err = sgmc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(sgmc.hooks) - 1; i >= 0; i-- {
			mut = sgmc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sgmc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (sgmc *SkipGenerationModelCreate) SaveX(ctx context.Context) *SkipGenerationModel {
	v, err := sgmc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (sgmc *SkipGenerationModelCreate) check() error {
	if _, ok := sgmc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("_example: missing required field \"name\"")}
	}
	return nil
}

func (sgmc *SkipGenerationModelCreate) sqlSave(ctx context.Context) (*SkipGenerationModel, error) {
	_node, _spec := sgmc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sgmc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (sgmc *SkipGenerationModelCreate) createSpec() (*SkipGenerationModel, *sqlgraph.CreateSpec) {
	var (
		_node = &SkipGenerationModel{config: sgmc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: skipgenerationmodel.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: skipgenerationmodel.FieldID,
			},
		}
	)
	if value, ok := sgmc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: skipgenerationmodel.FieldName,
		})
		_node.Name = value
	}
	return _node, _spec
}

// SkipGenerationModelCreateBulk is the builder for creating a bulk of SkipGenerationModel entities.
type SkipGenerationModelCreateBulk struct {
	config
	builders []*SkipGenerationModelCreate
}

// Save creates the SkipGenerationModel entities in the database.
func (sgmcb *SkipGenerationModelCreateBulk) Save(ctx context.Context) ([]*SkipGenerationModel, error) {
	specs := make([]*sqlgraph.CreateSpec, len(sgmcb.builders))
	nodes := make([]*SkipGenerationModel, len(sgmcb.builders))
	mutators := make([]Mutator, len(sgmcb.builders))
	for i := range sgmcb.builders {
		func(i int, root context.Context) {
			builder := sgmcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SkipGenerationModelMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, sgmcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, sgmcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, sgmcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (sgmcb *SkipGenerationModelCreateBulk) SaveX(ctx context.Context) []*SkipGenerationModel {
	v, err := sgmcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
